import { ID } from 'appwrite';
import { account } from '.';

export const createAccount = async (request: any) => {
  try {
    // await account.create(ID.unique(), email, password, name);
  } catch (error) {}
};
